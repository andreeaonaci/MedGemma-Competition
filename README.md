This is UTCN's repo for participating at the Medgemma competition
pip install -r req.txt in a python 3.10 env
connect to you hugging face with your token

run the script with:
streamlit run app.py
and in a separate cli run 
python -m api
